category: application
doc: |
  This is a test for the dimension keywords.
# replaced by NXdimensionsType
type: group
NXarchive(NXobject):
  (NXentry):
    my_dim_array(NX_NUMBER):
      doc: Test
      dim: (2,)
    my_dim_array2(NX_NUMBER):
      doc: Testarray with dimensions spelled out
      dimensions:
        rank: 1
        dim: (10, 30)
    my_dim_array3(NX_NUMBER):
      doc: Testarray with dimensions spelled out
      dimensions:
        rank: 1
        dim: (10,)
    my_dim_with_numpy_3d(NX_NUMBER):
      dim: (2, 4, 5)
    # this syntax is no longer supported
    # my_dim_with_doc_and_dim_attr(NX_NUMBER):
    #   dimensions:
    #     doc: |
    #       Test with doc and dim attribute
    #     dim: [[1, 2], [3, 4]]
    #     dim_parameters:
    #       ref: ['s', 'm']